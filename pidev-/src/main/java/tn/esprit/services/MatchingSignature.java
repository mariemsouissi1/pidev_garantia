package tn.esprit.services;
import org.opencv.core.Core;
import org.opencv.core.Mat;
import org.opencv.core.Size;
import org.opencv.imgcodecs.Imgcodecs;
import org.opencv.imgproc.Imgproc;
//import opencv as cv2;

public class MatchingSignature {
	
	public float match(String path1, String path2) {
//		System.loadLibrary(Core.NATIVE_LIBRARY_NAME);
//	    //read the images
////	    img1 = cv2.imread("path1");
//	    img2 = cv2.imread("path2");
//	    // turn images to grayscale
//	    img1 = cv2.cvtColor(img1, cv2.COLOR_BGR2GRAY);
//	    img2 = cv2.cvtColor(img2, cv2.COLOR_BGR2GRAY);
//	
//	    //resize images for comparison
//	    img1 = cv2.resize(img1,new Size(300,300));
//	    img2 = cv2.resize(img2,new Size(300,300));
//	    //display both images
//	    cv2.imshow("One", img1);
//	    cv2.imshow("Two", img2);
//	    cv2.waitKey(0);
//	    cv2.destroyAllWindows();
//	    float similarity_value = "{:.2f}".format(ssim(img1, img2)*100);
//	    //print("answer is ", float(similarity_value),
//	    //       "type=", type(similarity_value))
//	    return similarity_value;
		return 0;
	}

}
